# home-assistant
Widget for home assistant

## Development
This project uses TypeScript and Esbuild for bundling.

1. Install dependencies:
   ```bash
   npm install
   ```

2. Build the project:
   ```bash
   npm run build
   ```

Open `widget.html` in your browser.
